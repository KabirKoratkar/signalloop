# Server-only Amazon Bedrock configuration. Never expose this token to browser code.
AWS_BEARER_TOKEN_BEDROCK=
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.amazon.nova-2-lite-v1:0

# Set to false to force the deterministic strategy fallback even when a key exists.
BEDROCK_LIVE_ENABLED=true
